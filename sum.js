/**
 * Función que recibe dos números y retorna la suma de ambos.
 * @param {number} sum1 - Primer número a sumar.
 * @param {number} sum2 - Segundo número a sumar.
 * @returns {number} Resultado de la suma.
 */
function sum(sum1, sum2) {
  return sum1 + sum2;
}

export default sum;